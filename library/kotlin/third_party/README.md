# Third-party native SDKs

`litert-interpreter-1.4.2/` contains the LiteRT legacy C Interpreter headers
and Android shared libraries used by the native CPU and GPU runtimes. Its
upstream license files are retained alongside the binaries.
